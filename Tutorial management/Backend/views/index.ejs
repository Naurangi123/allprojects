<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title><%= tutorial ? 'Edit Tutorial' : 'Create Tutorial' %></title>
</head>
<body>
    <h1><%= tutorial ? 'Edit Tutorial' : 'Create Tutorial' %></h1>

    <form action="<%= tutorial ? '/tutorial/' + tutorial.id : '/tutorial' %>" method="POST">
        <div>
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= tutorial ? tutorial.title : '' %>" required>
        </div>
        <div>
            <label for="description">Description:</label>
            <textarea id="description" name="description" rows="4" required><%= tutorial ? tutorial.description : '' %></textarea>
        </div>
        <div>
            <label for="published">Published:</label>
            <input type="checkbox" id="published" name="published" <%= tutorial && tutorial.published ? 'checked' : '' %>>
        </div>
        <input type="submit" value="<%= tutorial ? 'Update Tutorial' : 'Create Tutorial' %>">
    </form>

</body>
</html>
